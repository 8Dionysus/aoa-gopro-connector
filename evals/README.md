# Local eval surface

Phase 0 uses ordinary contract and replay tests. Add a local eval only when a
stable scenario asks more than those tests can answer. Central proof intake
will consume bounded public-safe packets through `aoa-evals`; this directory
does not declare readiness or verdicts.
